import SiteShell from "./SiteShell";

/** See SiteShellWhite — the "subtle" background variant for the account routes. */
const SiteShellSubtle = () => <SiteShell mainBackground="subtle" />;

export default SiteShellSubtle;
